# ABUILD generated by spkg2abuild converter from network-manager-applet-0.8-2.spkg

# Package metadata
pkgname=network-manager-applet
pkgver=0.8.998
pkgbuild=1
arch="auto"

# Package description
shortdesc=("Gnome NetworkManager applet")
longdesc=("Gnome NetworkManager applet")

# Source URL
source=("http://ftp.gnome.org/pub/GNOME/sources/network-manager-applet/0.8/network-manager-applet-${pkgver}.tar.bz2")

# Tags
tags="xapps net-misc"

adddep="NetworkManager ppp>=2.4.5 polkit-gnome gnome-icon-theme libgnome-keyring libnotify"

# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--bindir=/usr/bin \
--sbindir=/usr/sbin \
--libexecdir=/usr/libexec \
--includedir=/usr/include \
--mandir=/usr/man \
--infodir=/usr/info \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--sysconfdir=/etc \
--localstatedir=/var \
--disable-maintainer-mode"

INSTALL_KEYS="GCONF_DISABLE_MAKEFILE_SCHEMA_INSTALL=1 DESTDIR=$pkgdir"
after_build() {
	install -m755 -d "${pkgdir}/usr/share/gconf/schemas"
	gconf-merge-schema "${pkgdir}/usr/share/gconf/schemas/${pkgname}.schemas" --domain nm-applet ${pkgdir}/etc/gconf/schemas/*.schemas
	rm -f ${pkgdir}/etc/gconf/schemas/*.schemas
}

