# ABUILD generated by spkg2abuild converter from NetworkManager-pptp-0.8-1.spkg

# Package metadata
pkgname=NetworkManager-pptp
pkgver=0.8.998
pkgbuild=1
arch="auto"

# Package description
shortdesc=("NetworkManager PPTP plugin")
longdesc=("NetworkManager PPTP plugin. Adds support for VPN connections.")

# Source URL
source=("http://ftp.gnome.org/pub/GNOME/sources/NetworkManager-pptp/0.8/NetworkManager-pptp-${pkgver}.tar.bz2")

# Tags
tags="xapps net-misc"

# Manually-added dependencies
adddep="pptp ppp>=2.4.5"

# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--bindir=/usr/bin \
--sbindir=/usr/sbin \
--libexecdir=/usr/libexec \
--includedir=/usr/include \
--mandir=/usr/man \
--infodir=/usr/info \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--sysconfdir=/etc \
--localstatedir=/var"

