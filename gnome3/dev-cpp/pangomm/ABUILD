# ABUILD generated by spkg2abuild converter from pangomm-2.26.0-2.spkg

# Package metadata
pkgname=pangomm
pkgver=2.27.1
pkgbuild=1
arch="auto"

tree=`echo $pkgver | cut -d . -f 1-2`
# Package description
shortdesc=("C++ interface for pango")
longdesc=("Pangomm is a C++ wrapper for the pango library. Originally part of gtkmm, pangomm provides convenient C++ interfaces for handling both the layout and internationalization of text in graphical applications.")

# Source URL
source=("http://ftp.gnome.org/pub/GNOME/sources/pangomm/${tree}/pangomm-${pkgver}.tar.bz2")

# Tags
tags="libs dev-cpp"

# Manually-added dependencies
adddep=""
before_build() {
	go_src_dir
	set -e
	sed -i -e 's/^doc_subdirs/#doc_subdirs/' Makefile.in
	sed -i -e 's/^doc_subdirs/#doc_subdirs/' Makefile.am
	set +e
}
# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--mandir=/usr/man \
--sysconfdir=/etc \
--docdir=/usr/doc \
--disable-documentation \
--localstatedir=/var"
after_build() {
	# Remove docs: there is internets now.
	rm -rf ${pkgdir}/usr/doc
	rm -rf ${pkgdir}/usr/share/doc
}

