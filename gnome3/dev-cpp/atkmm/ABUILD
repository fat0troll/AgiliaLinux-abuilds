# ABUILD generated by spkg2abuild converter from pangomm-2.26.0-2.spkg

# Package metadata
pkgname=atkmm
pkgver=2.22.2
pkgbuild=1
arch="auto"

tree=`echo $pkgver | cut -d . -f 1-2`
# Package description
shortdesc=("C++ interface for ATK")
longdesc=("atkmm is a C++ wrapper for the atk library.")

# Source URL
source=("http://ftp.gnome.org/pub/GNOME/sources/atkmm/${tree}/${pkgname}-${pkgver}.tar.bz2")

# Tags
tags="libs dev-cpp"

# Manually-added dependencies
adddep=""
before_build() {
	go_src_dir
	set -e
	sed -i -e 's/^doc_subdirs/#doc_subdirs/' Makefile.in
	sed -i -e 's/^doc_subdirs/#doc_subdirs/' Makefile.am
	set +e
}
# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--mandir=/usr/man \
--sysconfdir=/etc \
--docdir=/usr/doc \
--disable-documentation \
--localstatedir=/var"
after_build() {
	# Remove docs: there is internets now.
	rm -rf ${pkgdir}/usr/doc
	rm -rf ${pkgdir}/usr/share/doc
}

