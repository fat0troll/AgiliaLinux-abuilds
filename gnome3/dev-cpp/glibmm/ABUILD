# ABUILD generated by spkg2abuild converter from glibmm-2.22.1-1.spkg

# Package metadata
pkgname=glibmm
pkgver=2.27.97
pkgbuild=1
arch="auto"
tree=`echo $pkgver | cut -d . -f 1-2`

# Package description
shortdesc=("GLib C++ bindings")
longdesc=("GLib C++ bindings")

# Source URL
source=("http://ftp.gnome.org/pub/GNOME/sources/glibmm/$tree/glibmm-${pkgver}.tar.bz2")

# Tags
tags="dev-cpp libs"

# Manually-added dependencies
adddep=""

# Build system
BUILD_SYSTEM="autotools"
BUILD_KEYS="--prefix=/usr \
--libdir=/usr/lib${LIBDIRSUFFIX} \
--mandir=/usr/man \
--docdir=/usr/doc \
--sysconfdir=/etc \
--localstatedir=/var"

after_build() {
	# Remove docs: there is internets now.
	rm -rf ${pkgdir}/usr/doc
	rm -rf ${pkgdir}/usr/share/doc
}
